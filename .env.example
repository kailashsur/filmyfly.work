# Example environment variables for FilmyFly backend
# Copy this file to `.env` and update values before running the app

PORT=3000
NODE_ENV=development

# Postgres connection URL (example)
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgres://user:password@localhost:5432/filmyfly_db

# Session secret for express-session
SESSION_SECRET=change_this_to_a_random_secret

# Firebase (if using Firebase features) - point to service account file path or JSON
# FIREBASE_CREDENTIALS=/path/to/firebase-service-account.json
