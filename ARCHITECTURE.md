# FilmyFly Deployment Architecture

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                          INTERNET USERS                             │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                    HTTP/HTTPS Traffic
                             │
           ┌─────────────────▼──────────────────┐
           │                                    │
           │     Domain: filmyflyhd.space      │
           │     (DNS → GCP Static IP)         │
           │                                    │
           └────────────────┬────────────────────┘
                             │
           ┌─────────────────▼──────────────────────────────────────┐
           │         GCP COMPUTE ENGINE VM (e2-medium)              │
           │         Ubuntu 22.04 LTS | 2vCPU | 4GB RAM + 4GB Swap │
           └─────────────────┬──────────────────────────────────────┘
                             │
          ┌──────────────────┴──────────────────┐
          │                                     │
          ▼                                     ▼
    ┌──────────────┐                   ┌──────────────┐
    │   Port 80    │                   │  Port 443    │
    │   (HTTP)     │                   │  (HTTPS)     │
    │ Redirects    │                   │  TLS 1.2/1.3 │
    │ to HTTPS     │                   │              │
    └──────┬───────┘                   └──────┬───────┘
           │                                  │
           └──────────────┬───────────────────┘
                          │
          ┌───────────────▼────────────────┐
          │                                │
          │    NGINX Reverse Proxy         │
          │  (Ultra-Fast Caching)          │
          │                                │
          │  Cache Config:                 │
          │  - Static: 1 year              │
          │  - Pages: 30 minutes           │
          │  - API: 5 minutes              │
          │  - Size: 1GB max               │
          │                                │
          │  Security Headers:             │
          │  - HSTS                        │
          │  - CSP Headers                 │
          │  - X-Frame-Options             │
          │                                │
          │  Compression:                  │
          │  - Gzip enabled                │
          │  - ~70% bandwidth reduction    │
          │                                │
          └───────────────┬────────────────┘
                          │
                    Port 3000
                          │
          ┌───────────────▼────────────────┐
          │                                │
          │    Node.js Application         │
          │   (Express.js + EJS)           │
          │                                │
          │  PM2 Cluster Mode:             │
          │  - 2 worker processes          │
          │  - Load balanced               │
          │  - Auto-restart on crash       │
          │  - Memory limit: 500MB each    │
          │                                │
          │  Running:                      │
          │  - EJS View Rendering          │
          │  - Route Handling              │
          │  - Business Logic              │
          │  - Database Queries            │
          │                                │
          └───────────────┬────────────────┘
                          │
          ┌───────────────▼──────────────────────┐
          │                                      │
          │    Supabase PostgreSQL Database     │
          │    (Remote - Cloud Connection)      │
          │                                      │
          │    Data:                            │
          │    - Users                          │
          │    - Movies                         │
          │    - Categories                     │
          │    - Settings                       │
          │    - Logs                           │
          │                                      │
          └──────────────────────────────────────┘
```

---

## Data Flow Diagram

```
User Request
    │
    ▼
┌─────────────────────────────┐
│  Check Nginx Cache          │
│  (Reverse Proxy Cache)      │
└─────────────┬───────────────┘
              │
        ┌─────┴─────┐
        │           │
   CACHE HIT    CACHE MISS
   (Fast)       (First load)
        │           │
        │           ▼
        │      ┌──────────────────┐
        │      │ Request Node.js  │
        │      │  (Port 3000)     │
        │      └────────┬─────────┘
        │               │
        │               ▼
        │      ┌──────────────────────┐
        │      │ Express Route        │
        │      │ - Parse request      │
        │      │ - Run middleware     │
        │      │ - Execute handler    │
        │      └────────┬─────────────┘
        │               │
        │               ▼
        │      ┌──────────────────────┐
        │      │ Database Query       │
        │      │ (Prisma Client)      │
        │      │ → Supabase           │
        │      └────────┬─────────────┘
        │               │
        │               ▼
        │      ┌──────────────────────┐
        │      │ Render EJS Template  │
        │      │ - HTML Generation    │
        │      │ - Insert data        │
        │      └────────┬─────────────┘
        │               │
        │               ▼
        │      ┌──────────────────────┐
        │      │ Send Response        │
        │      │ (Store in cache)     │
        │      └────────┬─────────────┘
        │               │
        └───────┬───────┘
                │
                ▼
    ┌───────────────────────┐
    │  Compress (Gzip)      │
    │  ~70% reduction       │
    └───────────┬───────────┘
                │
                ▼
    ┌───────────────────────┐
    │  Add Security Headers │
    │  Add Cache Headers    │
    │  Add CORS Headers     │
    └───────────┬───────────┘
                │
                ▼
    ┌───────────────────────┐
    │  Send to Browser      │
    │  (HTTPS + HTTP/2)     │
    └───────────┬───────────┘
                │
                ▼
        Browser Caches
        (1 year for static,
         30 days for pages)
```

---

## Deployment Process

```
┌──────────────────────────────┐
│   Local Machine              │
│   (Your Computer)            │
├──────────────────────────────┤
│ 1. Make code changes         │
│ 2. Commit to Git             │
│ 3. Push to GitHub main       │
│    (git push origin main)    │
└─────────────┬────────────────┘
              │
              ▼
┌──────────────────────────────┐
│   GitHub Repository          │
│   (kailashsur/filmyfly.work) │
│   (Private repo)             │
└─────────────┬────────────────┘
              │
      ┌───────┴────────┐
      │                │
   Option 1         Option 2
   Manual Deploy    Auto Deploy
      │                │
      ▼                ▼
┌────────────┐   ┌──────────────────┐
│ SSH to     │   │ GitHub Actions   │
│ GCP Server │   │ Workflow         │
│            │   │ (deploy.yml)     │
│ Run:       │   │                  │
│ /deploy.sh │   │ Auto triggers on │
│            │   │ push to main     │
└────┬───────┘   └────────┬─────────┘
     │                    │
     └────────┬───────────┘
              │
              ▼
     ┌─────────────────────┐
     │  GCP VM             │
     │  (Production Server)│
     ├─────────────────────┤
     │ 1. git pull         │
     │ 2. npm install      │
     │ 3. npm run build    │
     │ 4. Prisma generate  │
     │ 5. pm2 restart      │
     │ 6. Clear cache      │
     │ 7. Reload Nginx     │
     └────────┬────────────┘
              │
              ▼
     ┌─────────────────────┐
     │  LIVE! Updated at   │
     │  filmyflyhd.space   │
     │  (< 3 minutes)      │
     └─────────────────────┘
```

---

## File Structure on GCP Server

```
/home/filmyfly/
├── app/                           # Application directory
│   ├── src/                       # TypeScript source
│   │   ├── app.ts
│   │   ├── config/
│   │   ├── controllers/
│   │   ├── middleware/
│   │   ├── routes/
│   │   ├── types/
│   │   └── utils/
│   ├── dist/                      # Compiled JavaScript
│   ├── node_modules/              # Dependencies
│   ├── public/                    # Static files (served by Nginx)
│   │   ├── css/
│   │   ├── js/
│   │   └── images/
│   ├── prisma/                    # Database schema
│   │   ├── schema.prisma
│   │   └── migrations/
│   ├── views/                     # EJS templates
│   ├── logs/                      # Application logs
│   │   ├── out.log
│   │   └── err.log
│   ├── backups/                   # Database backups
│   ├── .env                       # Environment variables (SECRET!)
│   ├── package.json
│   ├── tsconfig.json
│   ├── ecosystem.config.js        # PM2 configuration
│   ├── deploy.sh                  # Deployment script
│   └── .git/                      # Git repository
│
├── backups/                       # Database backups
│   └── db_20251210_120000.sql
│
└── .ssh/                          # SSH keys
    ├── github                    # GitHub private key
    └── github.pub                # GitHub public key

/var/
├── cache/nginx/                  # Nginx cache
│   └── filmyfly/                 # Cached pages
│
└── log/nginx/                    # Nginx logs
    ├── access.log
    └── error.log

/etc/
├── nginx/                        # Nginx config
│   ├── sites-available/
│   │   └── filmyflyhd.space      # Your site config
│   └── sites-enabled/
│       └── filmyflyhd.space      # Symlinked
│
└── letsencrypt/                  # SSL certificates
    └── live/filmyflyhd.space/
        ├── fullchain.pem
        ├── privkey.pem
        └── cert.pem
```

---

## Performance Optimization Chain

```
┌─────────────────────────────────────────────────────────┐
│                   User Request                          │
└────────────────────┬────────────────────────────────────┘
                     │
        ┌────────────▼────────────┐
        │  Browser Cache Check    │      ✅ 1-year cache for
        │  (Client-side)          │         static assets
        └────────────┬────────────┘
                     │ [Cache Miss]
        ┌────────────▼─────────────┐
        │  CDN Cache Check         │      ✅ Optional: CloudFlare
        │  (Not configured)        │
        └────────────┬─────────────┘
                     │
        ┌────────────▼─────────────┐
        │  Nginx Cache Check       │      ✅ 30min for pages,
        │  (Server-side)           │         1yr for static
        └────────────┬─────────────┘
                     │ [Cache Miss - first visit]
        ┌────────────▼──────────────┐
        │  Compression (Gzip)       │     ✅ ~70% reduction
        │  enabled: on              │
        └────────────┬──────────────┘
                     │
        ┌────────────▼──────────────┐
        │  Node.js Cluster Mode     │     ✅ Multi-core
        │  (Load balanced)          │        utilization
        └────────────┬──────────────┘
                     │
        ┌────────────▼──────────────┐
        │  Database Connection      │     ✅ Supabase
        │  Pooling (Prisma)         │        manages pooling
        └────────────┬──────────────┘
                     │
        ┌────────────▼──────────────┐
        │  Store in Nginx Cache     │     ✅ Cache for
        │  (for next request)       │        future hits
        └────────────┬──────────────┘
                     │
        ┌────────────▼──────────────┐
        │  Send to Browser          │     ✅ HTTPS + HTTP/2
        │  (Compressed)             │        Multiplexing
        └────────────┬──────────────┘
                     │
        ┌────────────▼──────────────┐
        │  Browser Renders Page     │     ✅ Cached CSS/JS
        │  (Using cached assets)    │        already loaded
        └──────────────────────────┘

        💡 Result: < 200ms for cached requests
                  < 2s for first visit
```

---

## Security Architecture

```
┌────────────────────────────────────────────┐
│            Internet User                   │
└─────────────┬──────────────────────────────┘
              │
              ▼
    ┌─────────────────────────────┐
    │  TLS 1.3 Encryption         │  ✅ Secure channel
    │  (HTTPS)                    │     AES-256 encryption
    └─────────────┬───────────────┘
                  │
    ┌─────────────▼─────────────────────┐
    │  Security Headers Check            │
    │  ✅ HSTS (Strict-Transport-Sec)   │
    │  ✅ X-Content-Type-Options        │
    │  ✅ X-Frame-Options               │
    │  ✅ X-XSS-Protection              │
    └─────────────┬─────────────────────┘
                  │
    ┌─────────────▼─────────────────────┐
    │  Reverse Proxy (Nginx)            │
    │  - Hides Node.js server           │
    │  - DDoS mitigation                │
    │  - Request validation             │
    └─────────────┬─────────────────────┘
                  │
    ┌─────────────▼──────────────────┐
    │  Non-Root User (filmyfly)      │
    │  - Limited file permissions    │
    │  - No sudo access              │
    │  - Isolated processes          │
    └─────────────┬──────────────────┘
                  │
    ┌─────────────▼──────────────────────┐
    │  Session Authentication            │
    │  - HttpOnly cookies               │
    │  - Secure flag (HTTPS only)       │
    │  - 24-hour expiry                 │
    └─────────────┬──────────────────────┘
                  │
    ┌─────────────▼───────────────────┐
    │  Database Connection (Supabase)  │
    │  - SSL encrypted                 │
    │  - Firewall protected            │
    │  - Credentials in .env only      │
    └──────────────────────────────────┘
```

---

## Monitoring & Alerting

```
┌────────────────────────────────────────┐
│        PM2 Monitoring                  │
├────────────────────────────────────────┤
│                                        │
│  Real-time metrics:                    │
│  ├─ CPU Usage                          │
│  ├─ Memory Usage                       │
│  ├─ Process Restart Count              │
│  ├─ Uptime                             │
│  └─ Request Rate                       │
│                                        │
│  Commands:                             │
│  pm2 status     → Quick status         │
│  pm2 logs       → View logs            │
│  pm2 monit      → Real-time dashboard  │
│                                        │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│     System Metrics                     │
├────────────────────────────────────────┤
│                                        │
│  Memory:                               │
│  free -h                               │
│  ├─ RAM: 4GB                           │
│  └─ Swap: 4GB (prevents OOM)           │
│                                        │
│  Disk:                                 │
│  df -h                                 │
│  ├─ Boot: ~10GB used of 30GB           │
│  └─ Cache: Auto-rotated                │
│                                        │
│  CPU:                                  │
│  htop                                  │
│  ├─ Load average                       │
│  └─ Per-process usage                  │
│                                        │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│     Nginx Cache Statistics             │
├────────────────────────────────────────┤
│                                        │
│  Cache Size: du -sh /var/cache/nginx   │
│  ├─ Target: ~500MB - 1GB               │
│  └─ Auto-cleanup after 60 days         │
│                                        │
│  Cache Hit Rate:                       │
│  curl -I URL | grep X-Cache-Status     │
│  ├─ HIT = cached (fast!)               │
│  └─ MISS = fresh (first load)          │
│                                        │
│  Performance:                          │
│  curl -w "%{time_total}s" -o /dev/null │
│  └─ Target: < 200ms for cached         │
│             < 2s for first load        │
│                                        │
└────────────────────────────────────────┘
```

---

## Failover & Recovery

```
Application Crash
        │
        ▼
┌───────────────────────────┐
│  PM2 Detects Crash        │
│  (Auto-monitoring)        │
└───────────────┬───────────┘
                │
                ▼
    ┌───────────────────────┐
    │ Attempt Auto-Restart  │
    │ (Max 10 restarts)     │
    └───────────┬───────────┘
                │
        ┌───────┴────────┐
        │                │
    Success          Failure
        │                │
        ▼                ▼
    Running    ┌──────────────────┐
           │  Manual intervention │
           │  pm2 restart         │
           │  or                  │
           │  Check logs          │
           │  Fix issue           │
           └──────────────────────┘

    Meanwhile: Nginx serves cached pages
               Users see stale content
               No complete outage!
```

---

This architecture ensures:
✅ **Ultra-fast performance** with multi-layer caching
✅ **High availability** with auto-restart and swap space
✅ **Security** with TLS, headers, and isolated users
✅ **Easy maintenance** with automated deployment
✅ **Scalability** with cluster mode and resource limits
